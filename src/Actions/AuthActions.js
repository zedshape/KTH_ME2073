export const signIn = (credentials) => {
    return (dispatch) => {
       

    }
}

export const signOut = () => {
    return (dispatch) => {
        
    }
}

export const signUp = (newUser) => {
    return (dispatch) => {
        
    }
}
